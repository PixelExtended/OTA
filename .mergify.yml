pull_request_rules:
  - name: Automatic merge on approval
    conditions:
      - files~=^builds/
      - files~=^changelogs/
      - "check-success=Validate JSONs"
      - "#files=2"
    actions:
      merge:
        method: merge
